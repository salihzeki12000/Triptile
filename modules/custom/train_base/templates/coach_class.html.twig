{#
/**
 * @file coach_class.html.twig
 * Default theme implementation to present Coach class data.
 *
 * This template is used when viewing Coach class pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_coach_class()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('coach_class') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
