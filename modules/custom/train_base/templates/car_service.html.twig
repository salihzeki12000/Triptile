{#
/**
 * @file car_service.html.twig
 * Default theme implementation to present Car service data.
 *
 * This template is used when viewing Car service pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_car_service()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('car_service') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
