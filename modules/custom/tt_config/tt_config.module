<?php

/*
 * Implements hook_theme().
 */
function tt_config_theme($existing, $type, $theme, $path) {
  return array(
    'top_right_menu_block' => array('variables' => array()),
    'top_right_mobile_menu' => array('variables' => array())
  );


}